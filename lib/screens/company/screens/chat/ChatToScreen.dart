import 'package:flutter/material.dart';
class ChatToScreen extends StatefulWidget {
  const ChatToScreen({Key key}) : super(key: key);

  @override
  _ChatToScreenState createState() => _ChatToScreenState();
}

class _ChatToScreenState extends State<ChatToScreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
