import 'package:flutter/material.dart';
class MainDrawer extends StatelessWidget {
  Function(int) onClick;
  MainDrawer({this.onClick});
  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
