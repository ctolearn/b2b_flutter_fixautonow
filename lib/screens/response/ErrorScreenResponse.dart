import 'package:flutter/material.dart';
class ErrorScreenResponse extends StatelessWidget {
  const ErrorScreenResponse({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Error Check your connection"),);
  }
}
